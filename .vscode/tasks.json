{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "g++",
            "command": "g++ ${fileDirname}/main.cpp -o ${fileDirname}/main -lglut -lGLEW  -lGL -lSDL2_image `pkg-config --cflags --libs sdl2`",
            "args": [],
            "options": {
                "cwd": "/usr/bin"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}